# Copy to .env at the repo root. The build/flash/monitor/doctor scripts load it
# automatically if present. .env is gitignored.

IDF_PATH="${HOME}/esp/esp-idf"

# Linux native USB port:
PORT="/dev/ttyACM0"

# Windows example:
# PORT="COM3"
